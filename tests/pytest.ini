# Rooted here on purpose: the pack root has an __init__.py, and a rootdir above
# it would make pytest import the pack as a Package (relative imports fail).
[pytest]
addopts = --import-mode=importlib
